# **要件定義書: カスタム・ネイティブオートクリッカー (Chrome拡張機能)**

## **1\. プロジェクト概要**

### **1.1 背景・目的**

Webブラウザ上で動作するゲーム、Webアプリ、反復作業等の自動化・効率化を目的とする。

通常のJavaScriptイベント合成（dispatchEvent）では検知・無効化される環境（Canvas描画、isTrusted \=== true 必須のシステム等）に対応するため、Chrome DevTools Protocol（CDP）を利用したOSネイティブレベルの入力シミュレーションを行う。

### **1.2 前提条件・制約**

* **配布形態**: 個人利用専用（Chrome Web Store非公開、デベロッパーモードによるローカル読み込み）。  
* **プラットフォーム**: Google Chrome（PC版、Manifest V3対応）。  
* **特記事項**:  
  * CDP（chrome.debugger）接続時、ブラウザ上部に「"\[拡張機能名\]" がブラウザのデバッグを開始しました」という警告バーが表示される仕様を許容する。  
  * 操作対象タブが最前面（アクティブ）である必要がある。

## **2\. システムアーキテクチャ & コンポーネント構成**

### **2.1 アーキテクチャ図**

┌─────────────────────────────────────────────────────────────┐  
│                       Popup UI                              │  
│  (録画/再生/停止, ループ設定, プロファイル選択, JSON入出力)   │  
└──────────────┬───────────────────────────────┬──────────────┘  
               │ (UI操作メッセージ)              │ (設定・マクロ読み書き)  
               ▼                               ▼  
┌──────────────────────────────┐        ┌─────────────────────┐  
│  Background Service Worker   │ ◀────▶ │ Chrome Storage API  │  
│  (CDP制御, ループ実行, 停止管理) │        │ (local)             │  
└──────────────┬───────────────┘        └─────────────────────┘  
               │ (CDP: Input.dispatchMouseEvent)  
               │ (chrome.debugger)  
               ▼  
┌─────────────────────────────────────────────────────────────┐  
│                   ターゲット Webページ                       │  
│ ┌─────────────────────────────────────────────────────────┐ │  
│ │ Content Script (録画時のみ動的注入)                     │ │  
│ │ ・mousedown / mouseup / mousemove を監視・サンプリング  │ │  
│ └─────────────────────────────────────────────────────────┘ │  
└─────────────────────────────────────────────────────────────┘  
