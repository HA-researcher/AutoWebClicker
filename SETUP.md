# AutoWebClicker - 初期セットアップガイド

## プロジェクト構成

```
AutoWebClicker/
├── manifest.json              # Chrome拡張機能マニフェスト
├── package.json               # プロジェクト設定
├── .gitignore
├── README.md
├── 要件定義書_...md           # 要件定義ドキュメント
└── src/
    ├── popup/                 # ポップアップUI
    │   ├── popup.html
    │   ├── popup.css
    │   └── popup.js
    ├── background/            # バックグラウンドサービスワーカー
    │   └── background.js
    └── content-script/        # コンテントスクリプト
        └── content-script.js
```

## インストール & 実行方法

### 1. Chrome拡張機能として読み込む

1. Chrome を開く
2. `chrome://extensions/` にアクセス
3. 右上の「デベロッパーモード」をON
4. 「パッケージ化されていない拡張機能を読み込む」をクリック
5. このプロジェクトのディレクトリを選択

### 2. 拡張機能の使用方法

- **🔴 録画開始**: マウスイベントの記録を開始
- **▶ 再生**: 記録したマクロを再生（ループ回数設定可能）
- **⏹ 停止**: 録画または再生を停止

### 3. プロファイル管理

- **💾 保存**: 現在の記録をプロファイルとして保存
- **📂 読込**: 保存済みプロファイルを読み込み
- 保存済みプロファイルリストから削除も可能

### 4. インポート/エクスポート

- **📤 エクスポート (JSON)**: マクロを JSON ファイルとしてダウンロード
- **📥 インポート (JSON)**: JSON ファイルからマクロを読み込み

## アーキテクチャ概要

### 1. **Background Service Worker** (`src/background/background.js`)
- マクロの状態管理
- Chrome Debugger Protocol (CDP) による OS レベルの入力シミュレーション
- プロファイル/マクロのストレージ管理
- Popup からのコマンド処理

### 2. **Content Script** (`src/content-script/content-script.js`)
- ターゲットページでマウスイベントを監視
- 記録時にイベント情報をサンプリング
- Background Service Worker に通知

### 3. **Popup UI** (`src/popup/popup.*`)
- ユーザーが操作するインターフェース
- 録画/再生/停止コントロール
- プロファイル管理、JSON I/O

## 主な機能

✅ **マウスイベント記録**
- mousedown / mouseup / mousemove を記録
- タイムスタンプとイベント間隔も含む

✅ **OS ネイティブ入力シミュレーション**
- Chrome DevTools Protocol (CDP) を使用
- Canvas ベースのゲームや `isTrusted === true` が必須のシステムに対応

✅ **プロファイル管理**
- Chrome Storage API で複数のマクロを保存
- プロファイル名で管理可能

✅ **ループ実行**
- 記録したマクロを指定回数ループ再生

✅ **JSON インポート/エクスポート**
- マクロを JSON 形式でエクスポート
- 外部で編集したマクロを インポート可能

## 注意事項

⚠️ **対象タブがアクティブである必要があります**
- マクロ再生時、対象のタブが最前面（フォーカス）している状態にしてください

⚠️ **デバッグ警告表示**
- CDP 接続時、Chrome ブラウザ上部に「AutoWebClicker がブラウザのデバッグを開始しました」という警告が表示されます
- これは正常な動作です（要件にて許容）

⚠️ **個人使用のみ**
- このツールは個人利用目的でのみ使用してください
- Chrome Web Store への公開は予定していません

## 開発・拡張

### 今後の改善予定

- [ ] マウスイベント以外のイベント対応（キーボード入力等）
- [ ] マクロの分岐・条件分岐機能
- [ ] UI の改善とダークモード対応
- [ ] パフォーマンス最適化

### コードの主要な拡張ポイント

1. **イベント型の追加**: `src/background/background.js` の `executeEvent()` 関数を拡張
2. **CDP コマンドの追加**: `chrome.debugger.sendCommand()` に新しいパラメータを追加
3. **UI/UX 改善**: `src/popup/` のファイルを修正

## トラブルシューティング

### Q: 「パッケージ化されていない拡張機能を読み込む」が見つからない

→ Chrome の `chrome://extensions/` ページで右上の「デベロッパーモード」トグルを ON にしてください

### Q: マクロが再生されない

→ 対象のタブが**アクティブ（最前面）**になっていることを確認してください

### Q: コンテントスクリプトが機能しない

→ ページをリロードしてから試してください。`chrome://extensions/` でエラーログを確認できます。

---

**作成日**: 2026-08-30  
**バージョン**: 1.0.0
